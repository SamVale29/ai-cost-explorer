name: Pricing watch

on:
  schedule:
    - cron: '17 7 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.6.0
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts
      - name: Validate catalog
        run: pnpm data:validate
      - name: Check model coverage manifest
        run: pnpm data:check-model-coverage
      - name: Check benchmark contract
        run: pnpm benchmark:check-empty
      - name: Probe official source pages
        run: pnpm pricing:watch
      - name: Preserve pricing review report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pricing-watch-${{ github.run_id }}
          path: reports/pricing-watch.*
          if-no-files-found: warn
      - name: Explain benchmark policy
        run: pnpm benchmark
